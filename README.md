# 📅 MyRoomScheduler (공유 자원 예약 시스템)

### 1. 기획 의도 (Background)
> **"형제간의 평화를 지키고, 편입 합격을 위한 '절대 시간'을 확보하다"**

편입 시험 준비 중, 형과 좁은 방을 같이 쓰다 보니 동선이 겹쳐 공부 흐름이 끊기는 문제가 빈번했습니다. 
말로 하는 약속은 잊혀지거나 지켜지지 않았기에, **확실한 '공유 자원(방/책상) 예약 시스템'**을 직접 만들어 불필요한 마찰을 없애고자 개발했습니다.

* **핵심 가치:** 명확한 시간 점유를 통한 갈등 해결 및 학습 효율 증대
* **개발 기간:** 2025.11 ~ 진행 중 (현재 v1.0 MVP 단계)

### 2. 현재 기능 (Features v1.0)
현재는 핵심 기능인 **'예약 확정'**에 집중된 MVP(Minimum Viable Product) 버전입니다.
* **✅ 일정 등록 (Create):** 사용자가 원하는 날짜와 시간에 방 사용 예약 (DB 저장)
* **✅ 일정 조회 (Read):** 저장된 예약 내역을 리스트 형태로 확인
* **✅ 데이터 보존:** SQLite를 연동하여 프로그램 종료 후에도 데이터 영구 보존
* **✅ 휴대성:** `Fat JAR` 빌드를 통해 별도 설치 없이 파일 하나로 즉시 실행 가능

### 3. 기술 스택 (Tech Stack)
* **Language:** Java 21 (LST)
* **Database:** SQLite (Embedded)
* **Build Tool:** Gradle
* **IDE:** IntelliJ IDEA

### 4. 트러블 슈팅 (Troubleshooting)
개발 과정에서 겪은 주요 문제와 해결 과정입니다.

#### 🔥 Issue 1: JAR 배포 시 DB 경로 문제
* **문제:** IDE에서는 잘 되던 데이터 저장이, JAR 파일로 실행하면 초기화되는 현상 발생.
* **원인:** SQLite가 실행 위치(Working Directory)를 기준으로 DB 파일을 찾기 때문.
* **해결:** 배포 시 `.db` 파일을 JAR와 같은 폴더에 위치시키도록 운영 가이드 수립.

#### 🔥 Issue 2: Gradle 빌드 인코딩 깨짐
* **문제:** 콘솔 출력 시 한글이 `???`로 깨짐.
* **해결:** `build.gradle`에 인코딩 옵션(`options.encoding = 'UTF-8'`)을 추가하여 해결.

---

### ⚠️ 향후 개선 계획 (Roadmap)
실사용 테스트를 통해 발견된 점들을 **v2.0**에서 보완할 예정입니다.
* **[Critical] 중복 예약 방지:** 현재는 시간 겹침 허용 -> 감지 알고리즘 추가 예정.
* **[Feature] 일정 삭제/수정:** 잘못 등록한 일정을 취소하는 기능 구현 예정.
